/*
 * Accounting Messages
 *
 * This contains all the text for the Accounting container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Accounting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Accounting container!',
  },
});
