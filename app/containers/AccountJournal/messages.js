/*
 * AccountJournal Messages
 *
 * This contains all the text for the AccountJournal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AccountJournal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AccountJournal container!',
  },
});
